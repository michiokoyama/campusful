query getArticles ($categoryIds: [Float!]!) {
  articles (categoryIds: $categoryIds){
    id
    title
    content
    published
    thanksNum
    commentNum
    createdAt
		author {
      id
      name
      email
      university {
        name
      }
    }
    category {
      name
    }
  }
}