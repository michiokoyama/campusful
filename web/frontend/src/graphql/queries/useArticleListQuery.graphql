query getArticles ($categoryIds: [Float!]!) {
  articles (categoryIds: $categoryIds){
    id
    title
    content
    published
		author {
      id
      name
      email
      university {
        name
      }
    }
    category {
      name
    }
  }
}