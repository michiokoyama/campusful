mutation CreateComment (
  $content: String!
  $articleId: Float!
  $authorId: Float!
) {
  createcomment(
    content: $content,
    articleId: $articleId,
    authorId: $authorId,
  ){
    content
 }
}